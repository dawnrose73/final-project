import React from 'react';
import './ReleaseButton.scss';

function ReleaseButton() {
    return (
        <button className="card__release-btn">Release</button>
    );
}

export default ReleaseButton;